import React from 'react';
import myLogo from '../../assets/images/logo192.png';

const logo = (props) => (
    <div>
        <img src={myLogo} className='Logo' alt='logo'></img>
    </div>
);

export default logo;